angular.module('app.page1')
.controller('page1Ctrl', ['$scope', '$rootScope', '$timeout', '$log', page1Ctrl]);

function page1Ctrl($scope, $rootScope, $timeout, $log) {
  
 

    activate();

    function activate() {
        
    }
}
